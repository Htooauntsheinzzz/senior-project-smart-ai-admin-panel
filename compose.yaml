include:
  - path: ../../backend/seniorproject/compose.yaml
    env_file: ../../backend/seniorproject/.env

name: smart-university

services:
  admin-web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080}
